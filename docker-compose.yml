services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    image: travel-aa-bill:latest
    container_name: travel-aa-bill
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "${SERVER_PORT}:${SERVER_PORT}"
